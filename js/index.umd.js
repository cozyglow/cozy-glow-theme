// Import Bootstrap's bundle (all of Bootstrap's JS + Popper.js dependency)

import '@epickris/bootstrap-kit/dist/js/kit.bundle';

import NavbarGlobal from './src/navbar-global';

export {
  NavbarGlobal
};
