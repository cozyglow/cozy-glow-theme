// Import Bootstrap Kit's bundle (all of Bootstrap's JS + Popper.js dependency)

import '@epickris/bootstrap-kit/dist/js/bootstrap-kit.bundle';

import NavbarGlobal from './navbar-global';

export {
  NavbarGlobal
};
